# shell: bash
# shellcheck disable=SC2034

# Deployment configuration
PORT="5342"
SITE_URL="https://api.example.com"
APP_DOMAIN="example.com"
OZY_DEPLOYMENT_PROFILE="self_host"

# Security & Performance
ALLOWED_ORIGINS="https://app.example.com,https://api.example.com"
# JWT Secret Key
JWT_SECRET=replace-with-a-64-byte-random-secret
# Public key for client-side usage (optional, auto-generated if empty)
ANON_KEY=replace-with-a-long-random-anon-key
# Service role key for server-to-server integration (optional but recommended)
SERVICE_ROLE_KEY=replace-with-a-long-random-service-role-key

# SMTP Configuration (Compatible with cPanel, AWS SES, Resend, SendGrid)
# Leave empty to use LogMailer (console only)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM="noreply@example.com"
RATE_LIMIT_RPS="50"
RATE_LIMIT_BURST="100"
BODY_LIMIT="10M"

# Database (PostgreSQL)
DATABASE_URL="postgres://ozyuser:replace-with-strong-password@localhost:5432/Ozydb?sslmode=verify-full"
DB_POOLER_URL=""
DB_HOST="localhost"
DB_PORT="5432"
DB_USER="ozyuser"
DB_PASSWORD="replace-with-strong-password"
DB_NAME="Ozydb"
DB_SSLMODE="verify-full"

# Application Settings
DEBUG="false"
# Set to true after replacing example.* domains, secrets, SMTP, and DATABASE_URL/DB_POOLER_URL with production values.
OZY_STRICT_SECURITY="false"
INITIAL_ADMIN_EMAIL=""
INITIAL_ADMIN_PASSWORD=""

# Install-to-play behavior:
# - If JWT_SECRET is empty, OzyBase auto-generates and stores it in .ozy_secret.
# - If ANON_KEY is empty, OzyBase auto-generates and stores it in .ozy_anon_key.
# - If SERVICE_ROLE_KEY is empty, OzyBase auto-generates and stores it in .ozy_service_role_key.
# - If ALLOWED_ORIGINS is empty, OzyBase auto-derives safe defaults from SITE_URL/APP_DOMAIN.
# - In Coolify/Azure, DATABASE_URL + SITE_URL + APP_DOMAIN are required, and DB_POOLER_URL is strongly recommended.
# - In docker-compose.install.yml, DATABASE_URL is auto-built from DB_USER/DB_PASSWORD/DB_NAME.
